import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  signoutButtonStyle: {
    padding: 7,
    alignSelf: 'flex-end',
    top: 5,
    right: 5,
  },
});

export default styles;
